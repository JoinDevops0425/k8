apiVersion: v1
kind: Pod

metadata: 
  name: env-demo
  labels: 
    purpose: env-demo

spec:
  containers:
    - name: nginx
      image: nginx
      env:
        - name: course
          value: "kubernetes"
        - name: trainer
          value: "Siva" 